[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    no_db: pure configuration test; skips database setup entirely
filterwarnings =
    error::DeprecationWarning:app.*
